version: '3.7'

services:

  identity:
    image: tiksn/lionize-identity-management-service
    networks:
      - lionize

  web-ui:
    image: ashotnazaryan45/lionize-web-ui:latest
    ports:
      - "8080:80"
    networks:
      - lionize

networks:
      lionize:
        driver: bridge